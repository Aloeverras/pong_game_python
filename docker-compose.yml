services:
  devcontainer:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        USERNAME : ${USERNAME}
        USERID : ${USERID}
        GROUPNAME : ${GROUPNAME}
        GROUPID : ${GROUPID}